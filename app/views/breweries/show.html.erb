<p id="notice"><%= notice %></p>

<p>
  <strong><%= @brewery.name %></strong>
  
</p>

<p>
 <i>Established at <%= @brewery.year %> </i>
</p>
	<p> Number of beers: <%= @brewery.beers.to_a.length %></p>

<p>
 <% @brewery.beers.each do |beer| %>
   <%=link_to beer.name, beer%>
 <% end %>
</p>
<p>
<% if @brewery.ratings.empty? %>
  has not yet been rated!
 <% elsif  @brewery.ratings.to_a.length==1%>
   has <%=  @brewery.ratings.to_a.length %> rating average <%=@brewery.rating_average%> 
<% else %>
  has <%=  @brewery.ratings.to_a.length %> ratings average <%=@brewery.rating_average%> 
<% end %>
</p>
<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
